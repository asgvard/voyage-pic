import React from 'react';
import View from './partials/FlexView';

const styles = {
  content: {
    width: '100%',
    height: '100%'
  }
};

const Partners = () => <View style={styles.content}>{'Partners'}</View>;

export default Partners;
